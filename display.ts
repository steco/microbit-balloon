/**
 * Utility functions for controlling a balloon with a Micro:bit
 */

//% weight=100 color=#0fbc11 icon="\uf1ec" 
namespace balloon {
    /**
     * Parse an instruction received over the radio
     * Instruction is expected to contains [fb][ud][lr]
     * @param instruction the instruction
     */
    //% block
    export function parse(instruction: string) {
    }

}
